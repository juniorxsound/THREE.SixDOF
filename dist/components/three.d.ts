/**
 * A small wrapper for THREE imports so rollup tree-shakes only the parts we need better
 */
export { Object3D, ShaderMaterial, TextureLoader, BackSide, Mesh, Points, SphereBufferGeometry, Texture, IcosahedronBufferGeometry } from 'three';
